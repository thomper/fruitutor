#:import menubar menubar
<MenuBar>:
    orientation: 'horizontal'
    padding: 5

    Button:
        id: button_exit
        text: 'Exit'
        on_press: root.exit()

    Button:
        id: button_mirror
        text: 'Mirror'
        on_press: root.mirror()

    Button:
        id: button_lesson
        text: 'Lesson'
        on_press: root.lesson()
